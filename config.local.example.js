// Copy this file to config.local.js (gitignored) and fill in your real values.
// It pre-fills the backend URL/API key on first load in this browser, so you
// don't need to paste #setupApiUrl=...&setupApiKey=... into the URL each time.
window.POS_LOCAL_CONFIG = {
  apiUrl: "https://your-worker.example.workers.dev",
  apiKey: "your-api-token"
};
